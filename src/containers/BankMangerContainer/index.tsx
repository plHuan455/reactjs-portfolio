import * as React from 'react';

export interface BankManagerProps {
}

 const BankManagerContainer = (props: BankManagerProps) => {
  return (
    <div>
      bank manager
    </div>
  );
}

export default BankManagerContainer
