import { Story, Meta } from '@storybook/react';
import React from 'react';

import {{pascalCase name}}, { {{pascalCase name}}Props } from '.';

const meta: Meta = {
  title: 'Components/{{type}}/{{pascalCase name}}',
  component: {{pascalCase name}},
  argTypes: {},
};
export default meta;

export const Normal: Story<{{pascalCase name}}Props> = ({...args}) => {
  return <{{pascalCase name}} {...args} />
};

Normal.args = {
}
